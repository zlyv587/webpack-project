"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function demo(input) {
  if (!Array.isArray(input)) {
    throw new TypeError("Value of argument \"input\" violates contract, expected Array got " + (input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input)));
  }

  var items = input;
  items = [1, 2, 3];
  items = [4, 5, 6];
  items = makeArray();
  items = 456;
  items = 123;
  items;
  items = "wat";

  if (!(typeof items === 'string')) {
    throw new TypeError("Value of variable \"items\" violates contract, expected string got " + (items === null ? 'null' : (typeof items === "undefined" ? "undefined" : _typeof(items)) === 'object' && items.constructor ? items.constructor.name || '[Unknown Object]' : typeof items === "undefined" ? "undefined" : _typeof(items)));
  }

  items = makeString();

  items = "foo";

  if (!(typeof items === 'string')) {
    throw new TypeError("Value of variable \"items\" violates contract, expected string got " + (items === null ? 'null' : (typeof items === "undefined" ? "undefined" : _typeof(items)) === 'object' && items.constructor ? items.constructor.name || '[Unknown Object]' : typeof items === "undefined" ? "undefined" : _typeof(items)));
  }

  return items;
}

function makeArray() {
  return [7, 8, 9];
}

function makeString() {
  function _ref3(_id3) {
    if (!(typeof _id3 === 'string')) {
      throw new TypeError("Function \"makeString\" return value violates contract, expected string got " + (_id3 === null ? 'null' : (typeof _id3 === "undefined" ? "undefined" : _typeof(_id3)) === 'object' && _id3.constructor ? _id3.constructor.name || '[Unknown Object]' : typeof _id3 === "undefined" ? "undefined" : _typeof(_id3)));
    }

    return _id3;
  }

  return _ref3("foo bar");
}