"use strict";

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = test;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function zero() {
	function _ref(_id) {
		if (!(typeof _id === 'number')) {
			throw new TypeError("Function \"zero\" return value violates contract, expected number got " + (_id === null ? 'null' : (typeof _id === "undefined" ? "undefined" : _typeof(_id)) === 'object' && _id.constructor ? _id.constructor.name || '[Unknown Object]' : typeof _id === "undefined" ? "undefined" : _typeof(_id)));
		}

		return _id;
	}

	if (true) return _ref([].length);
}

function test() {
	return zero();
}