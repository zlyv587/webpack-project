"use strict";

var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var Bar = function Bar() {
  _classCallCheck(this, Bar);
};

var Foo = (function () {
  function Foo() {
    _classCallCheck(this, Foo);

    this._bar = new Bar();
  }

  _createClass(Foo, [{
    key: "setBar",
    value: function setBar() {
      var bar = null;

      if (!(bar == null || bar instanceof Bar)) {
        throw new TypeError("Value of variable \"bar\" violates contract, expected ?Bar got " + (bar === null ? 'null' : (typeof bar === "undefined" ? "undefined" : _typeof(bar)) === 'object' && bar.constructor ? bar.constructor.name || '[Unknown Object]' : typeof bar === "undefined" ? "undefined" : _typeof(bar)));
      }

      if (bar != null) {
        this._bar = bar;
      }
    }
  }]);

  return Foo;
})();

function demo() {
  var foo = new Foo();
  foo.setBar();
  return foo._bar;
}