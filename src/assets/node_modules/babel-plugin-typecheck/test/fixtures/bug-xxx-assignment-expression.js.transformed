"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var HEADER_OFFSET = 1;
var NEXT_OFFSET = 2;
var FIRST_BLOCK = 3;

function writeInitialHeader(int32Array) {
  if (!(int32Array instanceof Int32Array)) {
    throw new TypeError("Value of argument \"int32Array\" violates contract, expected Int32Array got " + (int32Array === null ? 'null' : (typeof int32Array === "undefined" ? "undefined" : _typeof(int32Array)) === 'object' && int32Array.constructor ? int32Array.constructor.name || '[Unknown Object]' : typeof int32Array === "undefined" ? "undefined" : _typeof(int32Array)));
  }

  var header = HEADER_OFFSET;
  var block = FIRST_BLOCK;
  int32Array[header + NEXT_OFFSET] = block;
}

function demo() {}