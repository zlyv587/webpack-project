"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

var _marked = [gen].map(regeneratorRuntime.mark);

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function demo(input) {
  function _ref(_id) {
    if (!Array.isArray(_id)) {
      throw new TypeError("Function \"demo\" return value violates contract, expected T[] got " + (_id === null ? 'null' : (typeof _id === "undefined" ? "undefined" : _typeof(_id)) === 'object' && _id.constructor ? _id.constructor.name || '[Unknown Object]' : typeof _id === "undefined" ? "undefined" : _typeof(_id)));
    }

    return _id;
  }

  var items = [];
  var it = gen();
  var next = undefined;
  while (!(next = it.next(input)).done) {
    var yieldedValue = next.value;
    items.push(yieldedValue);
  }
  return _ref(items);
}

function gen() {
  var _checkGenYield, _checkGenNext, last;

  return regeneratorRuntime.wrap(function gen$(_context) {
    while (1) switch (_context.prev = _context.next) {
      case 0:
        _checkGenNext = function _checkGenNext(_id4) {
          if (!(typeof _id4 === 'number')) {
            throw new TypeError("Generator \"gen\" received an invalid next value, expected number got " + (_id4 === null ? 'null' : (typeof _id4 === "undefined" ? "undefined" : _typeof(_id4)) === 'object' && _id4.constructor ? _id4.constructor.name || '[Unknown Object]' : typeof _id4 === "undefined" ? "undefined" : _typeof(_id4)));
          }

          return _id4;
        };

        _checkGenYield = function _checkGenYield(_id3) {
          if (!(typeof _id3 === 'number')) {
            throw new TypeError("Function \"gen\"  yielded an invalid value, expected number got " + (_id3 === null ? 'null' : (typeof _id3 === "undefined" ? "undefined" : _typeof(_id3)) === 'object' && _id3.constructor ? _id3.constructor.name || '[Unknown Object]' : typeof _id3 === "undefined" ? "undefined" : _typeof(_id3)));
          }

          return _id3;
        };

        last = 0;
        _context.next = 5;
        return _checkGenYield(1);

      case 5:
        _context.t0 = _context.sent;
        last = _checkGenNext(_context.t0);

        if (typeof last === 'number') {
          _context.next = 9;
          break;
        }

        throw new TypeError("Value of variable \"last\" violates contract, expected number got " + (last === null ? 'null' : (typeof last === "undefined" ? "undefined" : _typeof(last)) === 'object' && last.constructor ? last.constructor.name || '[Unknown Object]' : typeof last === "undefined" ? "undefined" : _typeof(last)));

      case 9:
        _context.next = 11;
        return _checkGenYield(2 + last);

      case 11:
        _context.t1 = _context.sent;
        last = _checkGenNext(_context.t1);

        if (typeof last === 'number') {
          _context.next = 15;
          break;
        }

        throw new TypeError("Value of variable \"last\" violates contract, expected number got " + (last === null ? 'null' : (typeof last === "undefined" ? "undefined" : _typeof(last)) === 'object' && last.constructor ? last.constructor.name || '[Unknown Object]' : typeof last === "undefined" ? "undefined" : _typeof(last)));

      case 15:
        _context.next = 17;
        return _checkGenYield(3 + last);

      case 17:
        _context.t2 = _context.sent;
        last = _checkGenNext(_context.t2);

        if (typeof last === 'number') {
          _context.next = 21;
          break;
        }

        throw new TypeError("Value of variable \"last\" violates contract, expected number got " + (last === null ? 'null' : (typeof last === "undefined" ? "undefined" : _typeof(last)) === 'object' && last.constructor ? last.constructor.name || '[Unknown Object]' : typeof last === "undefined" ? "undefined" : _typeof(last)));

      case 21:
        return _context.abrupt("return", true);

      case 22:
      case "end":
        return _context.stop();
    }
  }, _marked[0], this);
}