"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

var _exportType = require("./export-type");

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var User = _exportType.User,
    UserCollection = _exportType.UserCollection;
function demo(users) {
  function _ref(_id) {
    if (!User(_id)) {
      throw new TypeError("Function \"demo\" return value violates contract, expected User got " + (_id === null ? 'null' : (typeof _id === "undefined" ? "undefined" : _typeof(_id)) === 'object' && _id.constructor ? _id.constructor.name || '[Unknown Object]' : typeof _id === "undefined" ? "undefined" : _typeof(_id)));
    }

    return _id;
  }

  if (!UserCollection(users)) {
    throw new TypeError("Value of argument \"users\" violates contract, expected UserCollection got " + (users === null ? 'null' : (typeof users === "undefined" ? "undefined" : _typeof(users)) === 'object' && users.constructor ? users.constructor.name || '[Unknown Object]' : typeof users === "undefined" ? "undefined" : _typeof(users)));
  }

  return _ref(users[0]);
}