"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = demo;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

var Visitors = function Visitors(input) {
  return input != null && (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object';
};

var Visitor = function Visitor(input) {
  return typeof input === 'function';
};

function demo(visitor) {
  function _ref(_id) {
    if (!Visitors(_id)) {
      throw new TypeError("Function \"demo\" return value violates contract, expected Visitors got " + (_id === null ? 'null' : (typeof _id === "undefined" ? "undefined" : _typeof(_id)) === 'object' && _id.constructor ? _id.constructor.name || '[Unknown Object]' : typeof _id === "undefined" ? "undefined" : _typeof(_id)));
    }

    return _id;
  }

  if (!Visitor(visitor)) {
    throw new TypeError("Value of argument \"visitor\" violates contract, expected Visitor got " + (visitor === null ? 'null' : (typeof visitor === "undefined" ? "undefined" : _typeof(visitor)) === 'object' && visitor.constructor ? visitor.constructor.name || '[Unknown Object]' : typeof visitor === "undefined" ? "undefined" : _typeof(visitor)));
  }

  return _ref({
    foo: visitor
  });
}